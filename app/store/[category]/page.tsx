import React from 'react'

interface pageProps {
    params: {
        category: string
    }
}

const page = ({ params: { category } }: pageProps) => {
  return (
    <div>
      
    </div>
  )
}

export default page
